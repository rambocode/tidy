name = "tidy-telemetry"
main = "worker.js"
compatibility_date = "2026-01-01"

[vars]
# PostHog 的接入区域。欧洲项目改成 https://eu.i.posthog.com。
POSTHOG_HOST = "https://us.i.posthog.com"

# 部署后把自定义域绑上，然后用同一个地址构建桌面端：
#   TIDY_TELEMETRY_URL=https://<你的域名> make release
# [[routes]]
# pattern = "t.example.com"
# custom_domain = true
