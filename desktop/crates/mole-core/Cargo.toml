# mole-core: the safety layer. The ONLY crate allowed to perform deletions.
[package]
name = "mole-core"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
build = "build.rs"

[dependencies]
chrono.workspace = true
libc.workspace = true
serde.workspace = true
thiserror.workspace = true

[build-dependencies]
sha2 = "0.10"

[dev-dependencies]
tempfile.workspace = true
